<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
  * {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  }

  body {
    background: linear-gradient(135deg, #667eea, #764ba2);
    min-height: 100vh;
    padding: 2rem;
    color: white;
  }

  h1 {
    text-align: center;
    margin-bottom: 2rem;
  }

  .users {
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    justify-content: center;
    gap: 2rem;
  }

  .user {
    display: flex;
    flex-direction: row;
    align-items: center;
    gap: 1rem;
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
    border-radius: 15px;
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.2);
    padding: 1rem;
    width: 500px;
    max-width: 90%;
    transition: transform 0.3s;
  }

  .user:hover {
    transform: translateY(-5px);
  }

  .user img {
    width: 120px;
    height: 100px;
    object-fit: cover;
    border-radius: 10px;
    border: 2px solid white;
  }

  .user-info {
    flex: 1;
  }

  .user-info h3 {
    margin: 0.2rem 0;
  }

  .user-info h5 {
    margin-bottom: 0.5rem;
    font-weight: 400;
    color: #e0e0e0;
  }

  .button-group {
    margin-top: 0.5rem;
  }

  a.editbtn{
    background-color: #ebad0e;
    border-radius: 0.3rem;
    text-decoration: none;
    color: #fff;
  }

  a.deletebtn{
    background-color: red;
    border-radius: 0.3rem;
    text-decoration: none;
    color: #fff;
  }

  .btn {
    display: inline-block;
    padding: 0.5rem 1rem;
    text-decoration: none;
    border-radius: 8px;
    font-size: 0.9rem;
    font-weight: 500;
    transition: 0.3s ease;
    background-color: greenyellow;
    margin: 1rem;
}

  .btn.edit {
    background-color: #fdd835; /* Yellow */
    color: #333;
  }

  .btn.edit:hover {
    background-color: #fbc02d;
  }

  .btn.delete {
    background-color: #dc3545; /* Red */
    color: white;
  }

  .btn.delete:hover {
    background-color: #a71d2a;
  }

  .home-link {
    text-align: center;
    margin-top: 2rem;
  }

  .home-link a {
    color: #fff;
    font-weight: bold;
    text-decoration: none;
    border: 2px solid white;
    padding: 0.5rem 1rem;
    border-radius: 10px;
    transition: 0.3s, color 0.3s;
  }

  .home-link a:hover {
    background: white;
    color: #333;
  }

  .homebtn{
    text-align: center;
  }
  @media (max-width: 600px) {
    .user {
      flex-direction: column;
      align-items: flex-start;
      width: 100%;
    }

    .user img {
      width: 100%;
      height: auto;
    }

    .btn {
      margin-bottom: 0.5rem;
      background-color: greenyellow;
      color: #fff;
    }
  }
</style>


</head>
<body>
    <div>
        <h1>List of Users</h1>
        <div class="users">
            <div class="user">

                <% users.forEach((user)=>{ %>

                    <div>
                        <div class="img">

                            <img src="<%= user.image %>" alt="">
                        </div>
                        
                        <div class="info">

                            <h3><%= user.name %></h3>
                            <h5><%= user.email %></h5>
                            <div class="button-group">
                                <a class="editbtn" href="/edit/<%= user._id %>">Edit User</a>
                                <a class="deletebtn" href="/delete/<%= user._id %>">Delete User</a>
                            </div>
                        </div>
                    </div>

                <% }); %>


            </div>
        </div>
    </div>
    <div class="homebtn">
        <a class="btn" href="/">Home</a>
    </div>
</body>
</html>